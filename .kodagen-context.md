# KODAGEN BUILD CONTEXT — compaction-proof source of truth.
# If your context was compacted and you are unsure what to build: RE-READ THIS FILE.
# Do NOT grep your transcript. Do NOT invent a brand. Do NOT re-derive the mode.
BRAND (IMMUTABLE — use verbatim everywhere; NEVER invent or rename): Iron Oak Distillery
INDUSTRY (the site must be about THIS): Distillery / Premium Spirits (whiskey)
BUILD MODE: fullsite
ROUTES REQUIRED: real app/about/page.tsx, app/services/page.tsx, app/work/page.tsx, app/contact/page.tsx (NOT folded into the homepage).
ARCHETYPE: a scrubbed-frames hero MUST use HeroScrollText with >=3 changing chapters (not a single fading <h1>).


────────── PRE-LOCKED INTAKE FIELDS — HARD GATE ──────────
The operator already picked these via the visual intake wizard. They are
NOT suggestions; they override the SKILL.md picker logic. Treat each as
already-rolled and write them straight into the variation manifest. Do
not regenerate, do not pick alternates, do not log "I chose X" reasoning
— the user chose for you.

  • **archetype = "G"** with render mode "scrub-cinematic" — DO NOT re-pick from `archetype-compositions.md`. Skip the "Picking an archetype" table. Write this id into the manifest verbatim.
  • **heroStyle = "Scroll-paced video"** — plain-English label the user picked. Treat the archetype + gRenderMode as the authoritative ids.
  • **colorPalette = "Bordeaux & Cream" (id: bordeaux-cream)** — DO NOT roll a color seed from `style-selection.md`. Use these exact swatches as the brand colors in `tailwind.config` + CSS variables. Swatches (in order — bg/surface/accent/contrast): #F8F1E9,#E0CFC4,#7A1F2B,#3D0D14.
  • **typographyPair = "Unifraktur Maguntia + Inter" (id: unifrakturmaguntia-inter)** — load these exact Google Fonts; do not pick alternates. Title font: Unifraktur Maguntia. Body font: Inter.

If any of these conflict with industry-typical picks from SKILL.md, the
locked field WINS. Only the fields NOT listed above remain for you to
roll per the normal picker rules (style, scene_variant, motion_variant,
card_variant, hero_overlay_variant, hero_text_pattern, hero_entrance,
header/footer/about/contact variants, etc.).
──────────────────────────────────────────────────────────

